Broadcaster Playlist Service

Quick start
- Install deps: `npm install`
- Run server: `node src/index.js`
- Run tests: `npm test`

Endpoints (current)
- `GET /health` -> `{ "status": "ok" }`

Notes
- Default database file is `./data/playlist.db` when running the server.
